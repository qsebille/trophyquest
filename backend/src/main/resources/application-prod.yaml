spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT:5432}/${PGDATABASE}?currentSchema=app
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    hibernate:
      ddl-auto: none

logging:
  file:
    name: /home/ec2-user/trophyquest/logs/backend.log
  level:
    root: INFO
    org.springframework: INFO