name: Run Tests for Node Lambdas

on:
  pull_request:
    branches:
      - main
    paths:
      - "psn_fetcher/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: psn_fetcher

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: psn_fetcher/package-lock.json

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build

      - name: Run tests
        run: npm test